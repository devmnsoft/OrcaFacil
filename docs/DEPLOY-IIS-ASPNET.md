# Deploy IIS ASP.NET Core

Execute `dotnet publish src/OrcaFacil.Web -c Release -o ./publish`, instale o ASP.NET Core Hosting Bundle, configure variáveis de ambiente, connection string PostgreSQL e App Pool sem código gerenciado.
