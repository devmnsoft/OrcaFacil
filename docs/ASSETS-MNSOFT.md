# Assets MNSOFT

Este PR não adiciona arquivos binários. Ilustrações ficam em SVG textual puro em `wwwroot/img/illustrations`. A logo real da MNSOFT deve ser copiada manualmente depois; enquanto isso, o sistema usa fallback textual premium.
