# Matriz de QA visual

Validar em 320, 375, 768, 1024 e 1440 px, além de zoom de 200%. Em cada rota conferir fundo, títulos, texto secundário, cards, campos e placeholders, links, botões, tabelas, badges, alertas, toasts, modais, vazio, foco, teclado e ausência de rolagem horizontal.

Rotas: `/`, `/ComoFunciona`, `/Auth/Register`, `/Auth/Login`, `/Onboarding`, `/Dashboard`, `/Profile`, `/Clients`, `/Clients/Create`, `/Clients/Details`, `/Services`, `/Templates`, `/Documents`, `/Documents/CreateBudget`, `/Documents/CreateReceipt`, `/Subscription`, `/Plans`, `/Support`, `/Termos`, `/Privacidade`, `/Cookies`, `/Admin/Dashboard`, `/Admin/Accounts`, `/Admin/Users`, `/Admin/Plans`, `/Admin/Payments` e `/Admin/Settings`.

Estado nesta execução: **não testado visualmente**, pois o runtime .NET não está instalado no ambiente. O script preventivo de contraste não substitui esta inspeção.
