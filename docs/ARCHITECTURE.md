# Arquitetura

O OrçaFácil segue Clean Architecture com `Domain`, `Application`, `Persistence`, `Infrastructure`, `Api` e `Web`. EF Core é usado para escrita transacional e Dapper para consultas de histórico/dashboard.
