import { createService } from './services.js';
import { generatePdf } from './pdf.js';
import { today, money, calcDocument, readFileAsDataUrl, escapeHtml, uid } from './utils.js';

let service, currentUser=null, profile={}, docs=[];
const $=sel=>document.querySelector(sel);
const $$=sel=>Array.from(document.querySelectorAll(sel));
const toast=(msg)=>{ $('#toastBody').textContent=msg; bootstrap.Toast.getOrCreateInstance($('#appToast')).show(); };

function itemTemplate(item={}){
  const id=uid();
  return `<div class="item-row" data-item-id="${id}"><div class="row g-2 align-items-end"><div class="col-md-5"><label class="form-label small">Descrição</label><input class="form-control item-desc" value="${escapeHtml(item.description)}" placeholder="Serviço ou produto"></div><div class="col-4 col-md-2"><label class="form-label small">Qtd</label><input class="form-control item-qty" type="number" min="0" step="0.01" value="${item.qty??1}"></div><div class="col-4 col-md-2"><label class="form-label small">Valor</label><input class="form-control item-unit" type="number" min="0" step="0.01" value="${item.unit??0}"></div><div class="col-4 col-md-2"><label class="form-label small">Desconto</label><input class="form-control item-discount" type="number" min="0" step="0.01" value="${item.discount??0}"></div><div class="col-md-1 d-grid"><button class="btn btn-outline-danger btn-sm btn-remove-item" type="button"><i class="bi bi-trash"></i></button></div></div></div>`;
}
function addItem(item){$('#itemsList').insertAdjacentHTML('beforeend',itemTemplate(item));bindItemEvents();updateTotals();}
function bindItemEvents(){ $$('.item-row input').forEach(i=>i.oninput=updateTotals); $$('.btn-remove-item').forEach(b=>b.onclick=e=>{e.currentTarget.closest('.item-row').remove();updateTotals();}); }
function getItems(){return $$('.item-row').map(row=>({description:row.querySelector('.item-desc').value.trim(),qty:Number(row.querySelector('.item-qty').value)||0,unit:Number(row.querySelector('.item-unit').value)||0,discount:Number(row.querySelector('.item-discount').value)||0})).filter(i=>i.description||i.qty||i.unit);}
function collectDoc(){return {id:$('#docId').value||'',type:$('#docType').value,number:$('#docNumberBadge').dataset.number||'',date:$('#docDate').value,issueDate:$('#docDate').value,validUntil:$('#validUntil').value,dueDate:$('#validUntil').value,clientName:$('#clientName').value.trim(),clientDoc:$('#clientDoc').value.trim(),clientDocument:$('#clientDoc').value.trim(),clientContact:$('#clientContact').value.trim(),clientPhone:$('#clientContact').value.trim(),clientEmail:'',clientCity:$('#clientCity').value.trim(),items:getItems(),notes:$('#notes').value.trim(),status:'rascunho'};}
function validateDoc(doc){if(!doc.clientName)throw new Error('Informe o nome do cliente.');if(!doc.items.length)throw new Error('Adicione pelo menos um item.');if(!doc.items.some(i=>i.description&&i.qty>0))throw new Error('Informe descrição e quantidade de pelo menos um item.');}
function updateTotals(){const totals=calcDocument({items:getItems()});$('#subtotalLabel').textContent=money.format(totals.subtotal);$('#discountLabel').textContent=money.format(totals.discount);$('#totalLabel').textContent=money.format(totals.total);}
function toggleType(){const isBudget=$('#docType').value==='orcamento';$$('.budget-only').forEach(el=>el.classList.toggle('d-none',!isBudget));}
async function ensureNumber(doc){if(!doc.number){doc.number=await service.nextNumber(doc.type);$('#docNumberBadge').dataset.number=doc.number;$('#docNumberBadge').textContent=`Nº ${doc.number}`;}return doc;}
function resetDoc(){ $('#docForm').reset(); $('#docId').value=''; $('#docType').value='orcamento'; $('#docDate').value=today(); $('#validUntil').value=''; $('#docNumberBadge').dataset.number=''; $('#docNumberBadge').textContent='Novo'; $('#itemsList').innerHTML=''; addItem({qty:1}); toggleType(); updateTotals(); }
function fillDoc(doc){$('#docId').value=doc.id||'';$('#docType').value=doc.type||'orcamento';$('#docDate').value=(doc.date||today()).slice(0,10);$('#validUntil').value=(doc.validUntil||'').slice(0,10);$('#clientName').value=doc.clientName||'';$('#clientDoc').value=doc.clientDoc||'';$('#clientContact').value=doc.clientContact||'';$('#clientCity').value=doc.clientCity||'';$('#notes').value=doc.notes||'';$('#docNumberBadge').dataset.number=doc.number||'';$('#docNumberBadge').textContent=doc.number?`Nº ${doc.number}`:'Novo';$('#itemsList').innerHTML='';(doc.items?.length?doc.items:[{qty:1}]).forEach(addItem);toggleType();updateTotals();showTab('docTab');}
function showTab(id){$$('.tab-panel').forEach(p=>p.classList.toggle('d-none',p.id!==id));$$('#tabs .nav-link').forEach(b=>b.classList.toggle('active',b.dataset.tab===id));if(id==='historyTab')renderHistory();}
async function loadProfile(){profile=await service.getProfile();$('#issuerName').value=profile.name||profile.businessName||'';$('#issuerDoc').value=profile.document||profile.documentNumber||'';$('#issuerPhone').value=profile.phone||'';$('#issuerEmail').value=profile.email||currentUser?.email||'';$('#issuerCity').value=profile.city||profile.address||'';$('#issuerPix').value=profile.pix||'';$('#issuerPlan').value=profile.plan||'free';if(profile.logo||profile.logoBase64){$('#logoPreview').src=profile.logo||profile.logoBase64;$('#logoPreview').classList.remove('d-none');}updatePlanUi();}
async function loadDocs(){docs=await service.listDocuments();renderHistory();}
function renderHistory(){const q=($('#historySearch')?.value||'').toLowerCase();const t=$('#historyType')?.value||'';const list=(docs||[]).filter(d=>(!t||d.type===t)&&JSON.stringify(d).toLowerCase().includes(q));const box=$('#historyList');if(!box)return;box.innerHTML=list.length?'':'<div class="text-center text-secondary py-4">Nenhum documento encontrado.</div>';list.forEach(d=>{const total=calcDocument(d).total;box.insertAdjacentHTML('beforeend',`<div class="history-card"><div class="d-flex flex-column flex-md-row justify-content-between gap-3"><div><span class="badge text-bg-${d.type==='recibo'?'success':'primary'}">${d.type==='recibo'?'Recibo':'Orçamento'} Nº ${d.number||'-'}</span><h5 class="mt-2 mb-1">${escapeHtml(d.clientName)}</h5><p class="text-secondary mb-0">${escapeHtml(d.date||'')} • ${money.format(total)}</p></div><div class="d-flex flex-wrap gap-2 align-content-start"><button class="btn btn-sm btn-outline-primary" data-action="open" data-id="${d.id}">Abrir</button><button class="btn btn-sm btn-outline-success" data-action="pdf" data-id="${d.id}">PDF</button><button class="btn btn-sm btn-outline-secondary" data-action="dup" data-id="${d.id}">Duplicar</button><button class="btn btn-sm btn-outline-danger" data-action="del" data-id="${d.id}">Excluir</button></div></div></div>`);});}
async function historyAction(e){const btn=e.target.closest('button[data-action]');if(!btn)return;const doc=docs.find(d=>d.id===btn.dataset.id);if(!doc)return;try{if(btn.dataset.action==='open')fillDoc(doc);if(btn.dataset.action==='pdf')generatePdf(doc,profile);if(btn.dataset.action==='dup'){const copy={...doc,id:'',number:'',clientName:`${doc.clientName} (cópia)`};fillDoc(copy);toast('Documento duplicado. Revise e salve.');}if(btn.dataset.action==='del'){if(confirm('Excluir este documento?')){await service.deleteDocument(doc.id);await loadDocs();toast('Documento excluído.');}}}catch(err){toast(err.message);} }
function updatePlanUi(){const plan=(profile?.plan||'free');$('#freeBanner')?.classList.toggle('d-none',plan==='pro');$('#proBadge')?.classList.toggle('d-none',plan!=='pro');$('#currentPlanLabel') && ($('#currentPlanLabel').textContent=plan==='pro'?'Pro':'Free');}
async function setLoggedIn(user){currentUser=user;$('#authView').classList.toggle('d-none',!!user);$('#dashboardView').classList.toggle('d-none',!user);$('#sessionBox').classList.toggle('d-none',!user);$('#currentUserLabel').textContent=user?`${user.displayName||user.email||'Usuário'}${user.demo?' (demo)':''}`:'';if(user){resetDoc();await loadProfile();await loadDocs();}}

async function boot(){
  service=await createService(); const info=await service.init();
  $('#modeAlert').textContent=service.mode==='firebase'?'Firebase oficial conectado: login real habilitado.':'Modo demonstração local com localStorage.';
  service.onAuth(setLoggedIn);
  $('#authForm').onsubmit=async e=>{e.preventDefault();try{await service.login($('#authEmail').value,$('#authPassword').value);toast('Login realizado.');}catch(err){toast(err.message);}};
  $('#btnRegister').onclick=async()=>{try{await service.register($('#authEmail').value,$('#authPassword').value,$('#authName').value.trim());toast('Conta criada.');}catch(err){toast(err.message);}};
  $('#btnDemo').onclick=async()=>{try{localStorage.setItem('orcafacil:demo-enabled','1'); service=await createService(); await service.init(); await service.demo(); await setLoggedIn(service.user);toast('Demonstração iniciada.');}catch(err){toast(err.message);}};
  $('#btnLogout').onclick=async()=>{await service.logout();await setLoggedIn(null);};
  $$('#tabs .nav-link').forEach(b=>b.onclick=()=>showTab(b.dataset.tab));
  $('#docType').onchange=toggleType; $('#btnAddItem').onclick=()=>addItem({qty:1}); $('#btnNewDoc').onclick=resetDoc;
  $('#btnSaveDoc').onclick=async()=>{try{let doc=collectDoc();validateDoc(doc);doc=await ensureNumber(doc);const saved=await service.saveDocument(doc);$('#docId').value=saved.id;await loadDocs();toast('Documento salvo com sucesso.');}catch(err){toast(err.message);}};
  $('#btnPdf').onclick=async()=>{try{let doc=collectDoc();validateDoc(doc);doc=await ensureNumber(doc);const saved=await service.saveDocument(doc);$('#docId').value=saved.id;await loadDocs();generatePdf(saved,profile);}catch(err){toast(err.message);}};
  $('#profileForm').onsubmit=async e=>{e.preventDefault();try{const logoFile=$('#issuerLogo').files[0];const logo=logoFile?await readFileAsDataUrl(logoFile):(profile.logo||'');profile={businessName:$('#issuerName').value.trim(),name:$('#issuerName').value.trim(),documentNumber:$('#issuerDoc').value.trim(),document:$('#issuerDoc').value.trim(),phone:$('#issuerPhone').value.trim(),email:$('#issuerEmail').value.trim(),address:$('#issuerCity').value.trim(),city:$('#issuerCity').value.trim(),pix:$('#issuerPix').value.trim(),plan:$('#issuerPlan').value,logoBase64:logo,logo};await service.saveProfile(profile);if(logo){$('#logoPreview').src=logo;$('#logoPreview').classList.remove('d-none');}updatePlanUi();toast('Emitente salvo.');}catch(err){toast(err.message);}};
  $('#issuerLogo').onchange=async e=>{const data=await readFileAsDataUrl(e.target.files[0]);if(data){$('#logoPreview').src=data;$('#logoPreview').classList.remove('d-none');}};
  $('#historyList').onclick=historyAction; $('#historySearch').oninput=renderHistory; $('#historyType').onchange=renderHistory; $('#btnRefreshHistory').onclick=loadDocs; $$('.btnProWhats').forEach(b=>b.onclick=()=>toast('Pagamento em breve. Fale com a MNSOFT para ativar seu plano Pro.')); 
  resetDoc();
}
boot();
