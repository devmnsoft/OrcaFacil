import { readFile } from 'node:fs/promises';
const files=['src/OrcaFacil.Domain/Entities/CommercialJourney.cs','src/OrcaFacil.Persistence/Configurations/CommercialJourneyConfiguration.cs','src/OrcaFacil.Web/Pages/Receipts/Details.cshtml','src/OrcaFacil.Web/Pages/Receipts/Details.cshtml.cs'];const text=(await Promise.all(files.map(x=>readFile(x,'utf8')))).join('\n');const rules=[[/não substitui (documento fiscal|nota fiscal)/i,'aviso fiscal ausente'],[/AccountId\s*==\s*account\.AccountId/,'recibo sem isolamento por conta'],[/CancellationReason/,'cancelamento sem motivo'],[/AccountId.*Number[\s\S]*IsUnique\(\)/,'numeração por conta sem unicidade']];const failures=rules.filter(([r])=>!r.test(text)).map(([,m])=>m);if(failures.length){console.error(failures.join('\n'));process.exit(1)}console.log('Conformidade de recibos validada.');
