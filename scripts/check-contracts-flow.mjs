import { readFile } from 'node:fs/promises';
const model=await readFile('src/OrcaFacil.Web/Pages/Contracts/Details.cshtml.cs','utf8');const entity=await readFile('src/OrcaFacil.Domain/Entities/RecurringContract.cs','utf8');const sql=await readFile('database/script_completop.sql','utf8');
const rules=[[/AnyAsync\(x => x\.AccountId == account\.AccountId && x\.ContractId == id && x\.Competence == competenceDate/,'geração não impede competência duplicada'],[/ContractStatus.*Active/,'status ativo ausente'],[/RecurringAmount/,'valor recorrente ausente'],[/ux_contract_payment_competence/,'índice único da competência ausente'],[/FinancialEntryOrigin\.Contract/,'cobrança não cria conta a receber']];const failures=rules.filter(([r])=>!r.test(model+'\n'+entity+'\n'+sql)).map(([,m])=>m);if(failures.length){console.error(failures.join('\n'));process.exit(1)}console.log('Recorrência validada: cobrança real, tenant e idempotência por competência.');
