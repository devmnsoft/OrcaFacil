@model dynamic
<header class="of-page-header">
  <div>
    <p class="of-eyebrow">@(Model?.Eyebrow ?? "OrçaFácil")</p>
    <h1>@(Model?.Title ?? ViewData["Title"])</h1>
    <p>@(Model?.Text ?? "Organize seus documentos profissionais em poucos passos.")</p>
  </div>
</header>
