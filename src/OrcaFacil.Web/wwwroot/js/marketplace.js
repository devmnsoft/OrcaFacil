(() => { const safeInit=(name,init)=>{try{init();}catch(error){console.error(`[Marketplace:${name}]`,error);}}; safeInit('confirmation',()=>{document.querySelectorAll('[data-marketplace-confirm]').forEach(form=>form.addEventListener('submit',event=>{const checkbox=form.querySelector('input[type="checkbox"]');if(!checkbox?.checked){event.preventDefault();checkbox?.focus();return;}const button=form.querySelector('button[type="submit"]');if(button){button.disabled=true;button.textContent='Instalando com segurança…';}}));}); })();
