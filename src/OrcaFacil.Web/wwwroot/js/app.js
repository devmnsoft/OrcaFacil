(() => { const money = v => (Number(v || 0)).toLocaleString('pt-BR',{style:'currency',currency:'BRL'}); document.addEventListener('submit', e => { const btn=e.target.querySelector('[data-loading],button[type="submit"]'); if(btn&&!btn.dataset.allowDouble){btn.dataset.originalText=btn.innerHTML;btn.innerHTML='Aguarde...';btn.disabled=true;}}); document.querySelectorAll('[data-copy]').forEach(b=>b.addEventListener('click',async()=>{await navigator.clipboard.writeText(b.dataset.copy||location.href);b.textContent='Copiado!';})); document.querySelectorAll('[data-confirm]').forEach(el=>el.addEventListener('click',e=>{if(!confirm(el.dataset.confirm||'Confirmar ação?'))e.preventDefault();})); const recalc=()=>{let subtotal=0;document.querySelectorAll('.item').forEach(row=>{const q=parseFloat(row.querySelector('.qty')?.value?.replace(',','.')||0);const p=parseFloat(row.querySelector('.price')?.value?.replace(',','.')||0);const d=parseFloat(row.querySelector('.discount')?.value?.replace(',','.')||0);const total=Math.max(0,q*p-d);subtotal+=total;const out=row.querySelector('[data-item-total]'); if(out) out.textContent=money(total);});document.querySelectorAll('[data-subtotal]').forEach(x=>x.textContent=money(subtotal));const disc=parseFloat(document.querySelector('[data-document-discount]')?.value||0);document.querySelectorAll('[data-total]').forEach(x=>x.textContent=money(Math.max(0,subtotal-disc)));}; document.addEventListener('input',e=>{if(e.target.matches('.qty,.price,.discount,[data-document-discount]'))recalc(); if(e.target.matches('[data-phone]'))e.target.value=e.target.value.replace(/\D/g,'').replace(/(\d{2})(\d{5})(\d{0,4}).*/,'($1) $2-$3');}); recalc(); })();
