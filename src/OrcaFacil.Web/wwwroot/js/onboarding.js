const digits=value=>value.replace(/\D/g,'');
const money=value=>{const raw=digits(value);return raw?new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(raw)/100):''};
document.querySelectorAll('[data-onboarding-form]').forEach(form=>form.addEventListener('submit',()=>{if(!form.checkValidity())return;const button=form.querySelector('[type="submit"]');if(!button)return;button.disabled=true;button.setAttribute('aria-busy','true');button.textContent=button.dataset.submitText||'Salvando…'}));
const firstError=document.querySelector('.field-validation-error,input.input-validation-error,select.input-validation-error,textarea.input-validation-error');if(firstError){const target=firstError.matches('input,select,textarea')?firstError:firstError.closest('.field')?.querySelector('input,select,textarea');target?.focus({preventScroll:true});target?.scrollIntoView({block:'center',behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'});}
document.querySelectorAll('[data-phone]').forEach(input=>input.addEventListener('input',()=>{const n=digits(input.value).slice(0,11);input.value=n.length>10?n.replace(/(\d{2})(\d{5})(\d{0,4})/,'($1) $2-$3'):n.replace(/(\d{2})(\d{4})(\d{0,4})/,'($1) $2-$3')}));
document.querySelectorAll('[data-money]').forEach(input=>{input.addEventListener('blur',()=>input.value=money(input.value));});
document.querySelectorAll('[data-preview-source]').forEach(input=>input.addEventListener('input',()=>{const target=document.querySelector(`[data-preview-target="${input.dataset.previewSource}"]`);if(target)target.textContent=input.dataset.money?money(input.value):input.value||target.dataset.fallback||'—';checkMargin()}));
const checkMargin=()=>{const price=document.querySelector('[data-preview-source="price"]');const cost=document.querySelector('[data-cost]');const warning=document.querySelector('[data-margin-warning]');if(!price||!cost||!warning)return;const p=Number(digits(price.value))/100,c=Number(digits(cost.value))/100;warning.textContent=c>p?'Atenção: o custo informado é maior que o preço.':'';warning.classList.toggle('margin-negative',c>p)};
document.querySelectorAll('[data-person-type]').forEach(select=>select.addEventListener('change',()=>{const label=document.querySelector('label[for$="Document"]');if(label)label.textContent=select.value==='1'?'CNPJ':'CPF'}));
